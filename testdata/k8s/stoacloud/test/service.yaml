apiVersion: v1
kind: Service
metadata:
  name: shaple-builder-test
  namespace: default
  labels:
    app: shaple-builder-test
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: web
  selector:
    app: shaple-builder-test
